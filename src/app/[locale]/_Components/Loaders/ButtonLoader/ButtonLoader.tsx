"use client";
import { ClipLoader } from "react-spinners";

export function ButtonLoader() {
    return (
        <ClipLoader
            color="currentColor"
            size={14}
        />
    );
}